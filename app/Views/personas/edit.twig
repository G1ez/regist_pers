<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Editar Persona</title>
</head>
<body>
    <h1>Editar Persona</h1>

    <a href="/personas">Volver a la Lista</a>

    {% if validation %}
        <ul style="color: red;">
            {% for error in validation.getErrors() %}
                <li>{{ error }}</li>
            {% endfor %}
        </ul>
    {% endif %}

    <form action="/personas/update/{{ persona.id }}" method="post">
        <label for="nombre">Nombre:</label>
        <input type="text" id="nombre" name="nombre" value="{{ persona.nombre }}"><br><br>

        <label for="email">Email:</label>
        <input type="email" id="email" name="email" value="{{ persona.email }}"><br><br>

        <label for="telefono">Teléfono:</label>
        <input type="text" id="telefono" name="telefono" value="{{ persona.telefono }}"><br><br>

        <button type="submit">Actualizar</button>
    </form>
</body>
</html>
